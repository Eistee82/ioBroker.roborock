name: Test and Release

# Run this job on all pushes and pull requests
# as well as tags with a semantic version
on:
  push:
    branches:
      - "main"
    tags:
      # normal versions
      - "v[0-9]+.[0-9]+.[0-9]+"
      # pre-releases
      - "v[0-9]+.[0-9]+.[0-9]+-**"
  pull_request: {}

# Cancel previous PR/branch runs when a new commit is pushed
concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

env:
  NPM_CONFIG_PREFER_OFFLINE: true
  NPM_CONFIG_AUDIT: false
  NPM_CONFIG_FUND: false

jobs:
  # Performs quick checks alongside the adapter test matrix
  check-and-lint:
    if: contains(github.event.head_commit.message, '[skip ci]') == false

    runs-on: ubuntu-latest

    steps:
      - uses: ioBroker/testing-action-check@v2
        with:
          node-version: '24.x'
          # Uncomment the following line if your adapter cannot be installed using 'npm ci'
          # install-command: 'npm install'
          # Linting happens further down instead of inside this action: `npm run lint` now also
          # lints the admin tab, which needs `src-tab/node_modules` to exist first.
          lint: false
      - name: TypeScript check
        run: npm run typecheck
      # The admin tab is its own Vite project, so it is installed, linted, type checked, tested
      # and built once here instead of in every cell of the adapter test matrix.
      - name: Install admin tab dependencies
        run: npm run install:tab
      - name: Lint (adapter and admin tab)
        run: npm run lint
      - name: TypeScript check (admin tab)
        run: npm run typecheck:tab
      - name: Test admin tab
        run: npm run test:tab
      - name: Build admin tab
        run: npm run build:tab

  # Runs adapter tests on all supported node versions and OSes
  adapter-tests:
    needs: check-and-lint
    if: contains(github.event.head_commit.message, '[skip ci]') == false

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        node-version: [22.x, 24.x]
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Cache ioBroker Controller / Temp
        uses: actions/cache@v6
        with:
          path: |
            C:\Users\runneradmin\AppData\Local\Temp\test-iobroker.roborock
            /tmp/test-iobroker.roborock
          key: iobroker-controller-${{ runner.os }}-${{ matrix.node-version }}

      - uses: ioBroker/testing-action-adapter@v1
        with:
          node-version: ${{ matrix.node-version }}
          os: ${{ matrix.os }}
          package-cache: 'npm'
          build: true
          # admin/tab.html and admin/assets are committed, so the matrix only regenerates the docs.
          build-command: 'npm run docs'

  # Deploys the final package to NPM
  deploy:
    needs: [check-and-lint, adapter-tests]

    # Trigger this step only when a commit on any branch is tagged with a version number
    if: |
      contains(github.event.head_commit.message, '[skip ci]') == false &&
      github.event_name == 'push' &&
      startsWith(github.ref, 'refs/tags/v')

    runs-on: ubuntu-latest

    # Write permissions are required to create Github releases
    permissions:
      contents: write # Needed to create Github releases
      id-token: write

    steps:
      - uses: ioBroker/testing-action-deploy@v1
        with:
          node-version: '24.x'
          # Uncomment the following line if your adapter cannot be installed using 'npm ci'
          # install-command: 'npm install'
          github-token: ${{ secrets.GITHUB_TOKEN }}

          # When using Sentry for error reporting, Sentry can be informed about new releases
          # To enable create a API-Token in Sentry (User settings, API keys)
          # Enter this token as a GitHub secret (with name SENTRY_AUTH_TOKEN) in the repository options
          # Then uncomment and customize the following block:
          #  sentry: true
          #  sentry-token: ${{ secrets.SENTRY_AUTH_TOKEN }}
          #  sentry-project: "iobroker-roborock"
          #  sentry-version-prefix: "iobroker.roborock"
          #  # If your sentry project is linked to a GitHub repository, you can enable the following option
          #  # sentry-github-integration: true
